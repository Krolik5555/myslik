# Карта кода «Мыслика»

Чтобы найти нужное место, не читая файлы целиком. Правишь код — правь карту в том же коммите.
Если карта разошлась с кодом — верить коду.

Пересобрать список символов: `python tools/dev.py map`

## Общая схема

```
app.py       мост JS↔Python, окно, релиз/обновление, Telegram, ИИ-эндпоинты
ai.py        весь ИИ: провайдеры (облако/локальный llama.cpp), захват, отчёт
ui/index.html   порядок скриптов + ?v= (поднимать при каждой правке фронта)
ui/styles.css   вся вёрстка; блоки помечены комментариями (.flow-* — полотно, .g-* — граф)
ui/js/*.js      фронт: классические скрипты, общая глобальная область, порядок важен
```

Порядок загрузки: **core → model → views → graph → overlays → draw → ai → report → main**.
Ниже по списку можно звать всё, что выше. Наоборот — нет.

## ui/js — где что

Размеры файлов здесь намеренно не записаны: их печатают `dev.py map` и `dev.py check`, а
записанные руками отстают от кода. Самые крупные — `overlays.js` и `graph.js`, их целиком
не читать.

| Файл | За что отвечает | Ключевые точки входа |
|---|---|---|
| `core.js` | утилиты, хранилище, даты, цвета, состояние `S`, запись, откат | `Store`, `persist`, `writeNow`, `flushSave`, `sanitizeState`, `makeDate`, `nextRepeat`, `undoStep` |
| `model.js` | разбор строки захвата, CRUD элементов, связи, иерархия | `parseCapture`, `captureText`, `addItem`, `deleteItem`, `toggleDone`, `childrenOfLive`, `recomputeHierarchy` |
| `views.js` | вкладки: Сегодня, Задачи, Папки, Календарь, Корзина | `render`, `renderNav`, `renderToday`, `renderTasks`, `_viewSnapshot`/`_viewRestore` |
| `graph.js` | вкладка «Заметки»: граф-паутина + список | `renderNotes`, `noteCard`, класс `Graph` |
| `overlays.js` | все окна поверх + ПОЛОТНО | `overlay`, `closeOverlays`, `onOverlayClose`, `uiConfirm`, `openItemEditor`, `openNoteReader`, `openSettings`, класс `FlowEditor` |
| `draw.js` | вкладка «Доска»: Excalidraw из `ui/vendor/` слоем на весь экран, ленивая загрузка, сцена в `S.draw` | `renderDraw`, `drawOpen`/`drawClose`, `drawLoadLib`, `drawMount`, `drawStash`, `drawFlush`, `drawRetheme`, `drawDestroy` |
| `ai.js` | «умный захват»: статус, настройки, предложения | `aiCheckStatus`, `aiToggle`, `aiRefineCapture`, `aiApply` |
| `report.js` | отчёт из выделенного (простой + через ИИ) | `buildReportText`, `openReportModal` |
| `main.js` | старт, глобальные события, палитра, тосты, обновления | `boot`, `tryBoot`, `wireGlobal`, `toast`, `doImport`, `autoCheckUpdate` |

## Класс Graph (`graph.js`) — вкладка «Заметки»

- **Жизненный цикл:** `build()` собирает узлы, `_tick()` — кадр, `_schedule()` — ЕДИНСТВЕННОЕ
  место планирования кадра, `pause`/`resume`/`destroy`.
- **Фон:** WebGL — `_initBgGL`, `_drawBgGL`, самопроверка `_glSelfTest`. Контекст кэшируется
  на canvas (`_glCache`), есть обработчики потери и восстановления.
- **Взаимодействие:** `_wire` (указатель), `_openPop`/`_openLinkPop` (меню), `startLink`/`_linkTo`
  (связи), `_renderTray` (лоток неразобранных), `copySelection`/`pasteClip`.
- **Камера:** `_applyTransform`, `_fitView`, `_tweenView`, `_pt` (экран → мир).

## Класс FlowEditor (`overlays.js`) — полотно

- **Данные схемы** лежат в самом элементе: `it.flow = {blocks, edges, view, seeded}`,
  нормализует `ensureFlow`. Типы блоков — `FLOW_TYPES`, порядок в панели — `FLOW_ORDER`
  (медиа `image`/`video` в него намеренно не входят).
- **История отмены — своя:** `_histInit`, `_histSnap`, `_histPush`, `_histApply`, `undo`, `redo`.
  Пишется в `save()`; `save(true)` — тихо, только камера.
- **Блоки:** `_newBlock` → `_afterAdd`, `renderBlocks`, `_buildBlock`, `_blockHTML`, `_autoGrow`.
- **Рамки-контейнеры:** членство выводится из геометрии — `_frameAt`, `_recomputeMembership`.
- **Связи:** `_addEdge`, `drawEdges`, `_orthoPts`/`_directPts`, `_startConnect`/`_endConnect`.
- **Медиа:** `_imageFromFile`, `_videoFromFile`, кадрирование `_enterCrop`/`_exitCrop`/`_cancelCrop`,
  показ `_applyImg` (crop-прямоугольник РАСТЯГИВАЕТСЯ на рамку, не cover).
- **Ввод:** `_wireStage` (весь указатель), `_onKey`/`_onPaste` на document, `_covered()` — страж
  «поверх открыто другое окно».
- **Прочее:** `_align` (выравнивание), `copyText`/`_serialize` (схема как текст), `_pickItem`
  (привязка блока к заметке).

## app.py

- **Мост:** класс `Api` — всё, что зовётся из JS как `window.pywebview.api.<метод>`.
  Данные: `load`, `save`, `backup`, `export_data`, `import_data`.
  Окно: `win_min/max/close/drag`. Обновление: `check_update`, `apply_update`.
  Telegram: `telegram_check` + `telegram_ack`. ИИ: `ai_*` — тонкие обёртки над `ai.py`.
- **Окно и система:** `_find_own_window` (по PID, не по заголовку), `_set_taskbar_icon`,
  `_set_taskbar_button_icon`, `_enable_frameless_resize`, `_selftest`.
- **Старт:** `main()` — каталоги, бэкап, ИИ, уборка `.part`, создание окна, `webview.start(icon=…)`.

## ai.py

- **Конфигурация:** `_load_config`/`_save_config`, провайдер `_read_provider`/`set_provider`,
  движок `set_backend`, модели `list_models`/`set_model`/`delete_model`.
- **Состояние:** `status()` — единственный источник правды для фронта (`available`, `reason`).
- **Работа:** `capture` (мысль → поля) и `report` (выделенное → текст), у каждой две ветки:
  `_*_local` (llama.cpp) и `_*_api` (облако).
- **Загрузка модели:** `_get_llm` — ленивая, помнит фактически импортированный движок.

## Инварианты

Единственный список — в `CLAUDE.md`, раздел «Инварианты кода». Здесь его нет намеренно: два
списка расходятся, и один из них начинает врать.

## Данные

`data/planner.json`: `{v, areas[], items[], links[], tags[], settings{}}`.
`links` — массивы `[a, b, длина?]`; хабы областей — `hub_<id>`.
Элемент: `id, kind(task|note|flow), title, body, area, status, due, repeat, priority, tags[],
created, updated, done, x, y, pin, parent, deleted, deletedAt, color`; у схем — `flow`.
